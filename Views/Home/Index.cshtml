@{
    ViewData["Title"] = "خانه";
    //@model IEnumerable<UserManagement.Controllers.User>

}



<div id="main-page" class="d-flex flex-column px-2 py-5 p-sm-5">
        <div class="d-flex flex-row gap-1 justify-content-center">
        <butotn class="btn btn-secondary" onclick="location.href='@Url.Action("Index", "User")'">کاربران </butotn>
        <butotn class="btn btn-outline-secondary" onclick="location.href='@Url.Action("Index", "Group")'">گروه ها </butotn>
        </div>
</div>